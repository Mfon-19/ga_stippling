# Fixtures

This directory is reserved for sample inputs and golden outputs used by tests, benchmarks, and future export workflows.

Planned contents:

- sample source images
- golden outputs for regression tests
- serialized snapshots for restart and export tests
